# Pointer

Canonical context lives in the `adamastorx` repo:
`.claude/PROJECT.md` and `.claude/WORKFLOW.md`.

This repo (`services`) = application concern. Primary agent:
`backend-engineer` (see `adamastorx/.claude/agents/backend-engineer.md`).
