# Runtime dependencies (backlog #80). Pinned to exact versions verified
# together during development (installed + imported + exercised against
# real example headlines and a real local Kafka broker) -- not a loose
# floor. Same versions clinvar-service (ADR 0019) already pinned for the
# packages this service shares with it, reused rather than drifting to a
# second version for no reason.
fastapi==0.140.0
uvicorn[standard]==0.51.0
confluent-kafka==2.15.0
# vaderSentiment (ADR 0029's own chosen scorer) -- ships its own lexicon
# file inside the package; no model download at import or score time.
vaderSentiment==3.3.2
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-confluent-kafka==0.65b0
prometheus-client==0.26.0
